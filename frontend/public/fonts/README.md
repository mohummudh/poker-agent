Place local webfont files here for zero-network font loading:
- press-start-2p.woff2
- vt323.woff2

The app preloads these paths from index.html and falls back to locally installed fonts if files are absent.
